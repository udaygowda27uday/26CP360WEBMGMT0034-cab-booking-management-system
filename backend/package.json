{
  "name": "cab-booking-management-system-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mvn -q -DskipTests package",
    "db:migrate": "java -jar target/cab-booking-management-system-backend-1.0.0.jar --factory.task=migrate",
    "db:seed": "java -jar target/cab-booking-management-system-backend-1.0.0.jar --factory.task=seed"
  }
}
